extends ../shared/_layout

block content
  div.well#fail-message
    h2 Application from this user already exists.

  style(type="text/css").
    #fail-message {
      text-align: center;
      height: 60vh;
      margin: 5vh 0;
    }