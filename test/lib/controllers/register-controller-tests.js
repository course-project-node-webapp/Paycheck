/* globals describe it*/

'use strict';

const chai = require('chai');
const sinon = require('sinon');

describe('registerController', () => {
  it('should test', () => {
    chai.expect(1).to.equal(1);
  });
});